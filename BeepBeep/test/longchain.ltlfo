# --------------------------------------------------------------
# Example of formula accepted by BeepBeep 1.5
# Paths inside quantifiers can be any XPath expression, and
# even contain references to previously quantified variables
# --------------------------------------------------------------
G (∀ x ∈ //p : (F (∃ y ∈ //q : x=y)))